import styled from "styled-components";

import BottomNav from "@material-ui/core/BottomNavigation";

export const BottomNavigation = styled(BottomNav)`
  && {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 50px;
  }
`;
